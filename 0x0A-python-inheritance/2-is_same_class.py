#!/usr/bin/python3
"""module description"""


def is_same_class(obj, a_class):
    """ checks of is instance

    Args:
        obj(any): the object
        a_class (type): class
    Returns:
        return
    """
    return (isinstance(obj, a_class))
